{
  "name": "zarishdocs",
  "version": "1.1.0",
  "type": "module",
  "private": true,
  "description": "ZarishDocs local research workspace with a static browser UI and Node test runner.",
  "scripts": {
    "test": "node --test \"src/**/*.test.js\" \"worker/**/*.test.js\"",
    "test:unit": "node --test \"src/**/*.test.js\" \"worker/**/*.test.js\"",
    "serve": "python3 -m http.server 8080 --bind 127.0.0.1",
    "dev": "python3 -m http.server 8080 --bind 127.0.0.1",
    "check": "bash -c 'for f in $(find src worker -name \"*.js\" -not -name \"*.test.js\"); do node --check \"$f\" || exit 1; done'",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.4.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
