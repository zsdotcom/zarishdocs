# Testing Strategy

## Frameworks
- **Unit Tests:** [REPLACE: unit test tool] *(e.g. Vitest)*
- **E2E Tests:** [REPLACE: E2E test tool] *(e.g. Playwright)*

## Rules & Requirements
- **Coverage:** Aim for [REPLACE: target]% code coverage on critical paths.
- **Before Commit:** Always run `[REPLACE: test command]` before verifying a task is complete.
- **Failures:** NEVER skip tests or mock out assertions to make a pipeline pass without Human approval. If an Agent breaks a test, the Agent must fix it.

## Execution
- Command to run all tests: `[REPLACE: test command]`
- Command to run a single test file: `[REPLACE: single-file test command]`
