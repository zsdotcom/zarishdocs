# Tech Stack & Tools

- **Frontend:** [REPLACE: framework and version] *(e.g. Next.js App Router)*
- **Backend:** [REPLACE: framework and version] *(e.g. Node.js / Express)*
- **Database:** [REPLACE: database and ORM] *(e.g. PostgreSQL with Drizzle ORM)*
- **Styling:** [REPLACE: styling library] *(e.g. Tailwind CSS + shadcn/ui)*
- **Authentication:** [REPLACE: auth tool] *(e.g. Supabase Auth or NextAuth)*

## Error Handling Pattern
```javascript
// Provide a canonical example of error handling for the AI to mimic.
// [REPLACE: example implementation]
```

## Styling & Component Examples
```tsx
// Provide a small example of combining UI components for the framework chosen.
// [REPLACE: example implementation]
```
