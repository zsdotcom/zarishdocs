## What does this PR do?

<!-- One or two sentences. -->

## Related issue

Closes #

## Checklist

- [ ] No `latest` tags introduced in any config
- [ ] No new runtime dependency or lockfile introduced (ZarishDocs stays zero-runtime-dep)
- [ ] `npm test` passes, `npm run check` is clean, Prettier formatting applied (`npm run format`)
- [ ] `python scripts/validate.py` passes
- [ ] Docs updated (`README.md`, `AGENTS.md`, `docs/`, or `agent_docs/`) if this changes behavior
- [ ] No secrets, keys, or PII included anywhere in this PR
