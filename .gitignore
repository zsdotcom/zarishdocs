# Dependencies
node_modules/

# Cloudflare / Wrangler
.wrangler/
dev.vars

# Secrets — never commit
.env
.env.*
!.env.example

# Local tool state
.vscode/
.idea/
*.local.json
.mcp.json

# Package managers — zero runtime deps; commit a lockfile if deps are ever added
pnpm-lock.yaml

# OS noise
.DS_Store
Thumbs.db

# Runtime artifacts
dist/
*.log
session-ses_010d.md
